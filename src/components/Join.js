import React from 'react';
import styled from 'styled-components';

const Join = props => {
  return(
    <JoinBox>
    </JoinBox>
  )
}

const JoinBox = styled.div`

`

export default Join;